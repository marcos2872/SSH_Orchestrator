# Permite o build script do esbuild (exigido pelo Vite).
# O pnpm 11 bloqueia build scripts de dependências por padrão; configs
# como onlyBuiltDependencies agora vivem apenas neste arquivo.
onlyBuiltDependencies:
  - esbuild
