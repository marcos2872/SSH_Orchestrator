# Permite o build script do esbuild (exigido pelo Vite).
# pnpm 11 bloqueia build scripts por padrão; allowBuilds substitui a
# antiga onlyBuiltDependencies (que não é mais lida no pnpm 11).
allowBuilds:
  esbuild: true
